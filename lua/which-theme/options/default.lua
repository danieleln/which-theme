-- default options for which-theme
return {
    themes_directory = 'colors/which-theme',

    default_theme = {
        name                   = nil,
        style                  = nil,
        transparent_background = false,
    },

    window = {
        --- @usage 'right'|'center'|'left'
        align = 'center',
    },
}

